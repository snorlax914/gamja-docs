fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
pydantic==2.9.2
pydantic-settings==2.6.0

# OCR
# PaddleOCR-VL 레이아웃 검출 엔진. paddleocr 가 자동 설치하지 않으므로 별도 명시.
#  - CPU:  pip install paddlepaddle
#  - GPU:  pip install paddlepaddle-gpu --extra-index-url https://www.paddlepaddle.org.cn/packages/stable/cu126/ --index-strategy unsafe-best-match
#          (CUDA 12.x 드라이버 필요. .env 의 PADDLE_DEVICE=gpu 와 함께 사용)
paddlepaddle-gpu
paddleocr[doc-parser]
pdf2image==1.17.0         # 별도로 시스템에 poppler 설치 필요 (PDF → 이미지 변환)
pillow==11.0.0
beautifulsoup4==4.14.3    # OCR 출력의 <table> HTML 단편 → 마크다운 표 변환

# LLM / Embedding
httpx==0.27.2
sentence-transformers==3.2.1

# Vector DB
qdrant-client==1.12.1

# Utils
python-dotenv==1.0.1
loguru==0.7.2
